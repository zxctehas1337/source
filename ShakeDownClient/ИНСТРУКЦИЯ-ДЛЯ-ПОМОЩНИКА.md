# 📋 ИНСТРУКЦИЯ ДЛЯ ПОМОЩНИКА

## Что нужно сделать

Заменить старый GUI на новый ClickGUI с категориями модулей.

---

## 🗑️ ШАГ 1: Удалить старые файлы

Удали эти файлы (если они есть):

```
ShakeDownClient/src/main/java/com/arizon/client/gui/screen/ColorPickerGui.java
ShakeDownClient/src/main/java/com/arizon/client/gui/screen/GuiTheme.java
ShakeDownClient/src/main/java/com/arizon/client/gui/screen/UltraPremiumGui.java
ShakeDownClient/src/main/java/com/arizon/client/module/modules/other/Interface.java
```

---

## ➕ ШАГ 2: Создать новый ClickGui

Создай файл:
```
ShakeDownClient/src/main/java/com/arizon/client/gui/screen/ClickGui.java
```

Содержимое файла прикреплено отдельно (файл `ClickGui.java`)

---

## ✏️ ШАГ 3: Обновить ArizonClientMod.java

В файле `ShakeDownClient/src/main/java/com/arizon/client/ArizonClientMod.java`

**Найди:**
```java
client.setScreen(new com.arizon.client.gui.screen.UltraPremiumGui());
```

**Замени на:**
```java
client.setScreen(new com.arizon.client.gui.screen.ClickGui());
```

---

## ✏️ ШАГ 4: Обновить KeyboardMixin.java

В файле `ShakeDownClient/src/main/java/com/arizon/client/mixin/KeyboardMixin.java`

**Найди:**
```java
import com.arizon.client.gui.screen.UltraPremiumGui;
```

**Замени на:**
```java
import com.arizon.client.gui.screen.ClickGui;
```

**И найди:**
```java
mc.setScreen(new UltraPremiumGui());
```

**Замени на:**
```java
mc.setScreen(new ClickGui());
```

---

## ✏️ ШАГ 5: Обновить ScreenMixin.java

В файле `ShakeDownClient/src/main/java/com/arizon/client/mixin/ScreenMixin.java`

**Найди ВСЕ упоминания:**
```java
com.arizon.client.gui.screen.UltraPremiumGui
```

**Замени на:**
```java
com.arizon.client.gui.screen.ClickGui
```

(Должно быть 2 места)

---

## 🔨 ШАГ 6: Собрать проект

```batch
cd ShakeDownClient
.\gradlew build
```

Если всё правильно - проект соберётся без ошибок.

---

## ✅ Готово!

После этого:
- GUI будет открываться по кнопке **RIGHT SHIFT**
- Будут категории: Combat, Movement, Visual, Player, World, Misc
- Можно перетаскивать категории мышкой (ЛКМ на заголовке)
- Можно сворачивать категории (ПКМ на заголовке)
- Клик по модулю включает/выключает его

---

## 🐛 Если ошибки

**Ошибка "cannot find symbol UltraPremiumGui":**
- Проверь что заменил все импорты в шагах 3-5

**Ошибка "cannot find symbol GuiTheme":**
- Проверь что удалил старые файлы из шага 1

**Другие ошибки:**
- Убедись что файл ClickGui.java создан правильно
- Проверь что все изменения применены

---

## 📦 Файлы для отправки

Отправь помощнику:
1. Этот файл (ИНСТРУКЦИЯ-ДЛЯ-ПОМОЩНИКА.md)
2. Файл ClickGui.java (полный код)

Всё остальное он сделает сам по инструкции!
